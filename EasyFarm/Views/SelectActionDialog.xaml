<dialogs:CustomDialog x:Class="EasyFarm.Views.SelectActionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:dialogs="clr-namespace:MahApps.Metro.Controls.Dialogs;assembly=MahApps.Metro"
        Title="Please select an action" Height="300" Width="300" Style="{StaticResource DialogStyle}">
    <dialogs:CustomDialog.DialogTop>
        <Grid Background="{StaticResource WindowTitleColorBrush}">
            <TextBlock 
                FontSize="{StaticResource DialogMessageFontSize}"
                Foreground="{StaticResource WhiteBrush}"
                Text="Available Actions"
                TextAlignment="Center"
                Margin="5"
            />
        </Grid>
    </dialogs:CustomDialog.DialogTop>
    <Grid>
        <StackPanel>
            <ListBox Name="AbilityListBox" />
            <Button Name="CompleteSelectionButton">Complete Selection</Button>
        </StackPanel>
    </Grid>
</dialogs:CustomDialog>

