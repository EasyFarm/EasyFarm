<UserControl x:Class="EasyFarm.Views.RoutesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="86.301" d:DesignWidth="454.11">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100" />
            <ColumnDefinition Width="300" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <!--Contains the list of coordinates to display to the user.-->
        <ListBox x:Name="lstWaypoints" MaxHeight="100" Width="Auto" Height="Auto" Grid.Column="1" HorizontalAlignment="Stretch" Margin="10,3,0,-11" VerticalAlignment="Stretch" Padding="10" Grid.Row="1"/>
        <!--Begins record waypoints and adding them to the list.-->
        <Button x:Name="btnRecordWaypoints" Click="RecordRoute"  HorizontalAlignment="Left" Content="Record" Margin="10,10,0,20" Grid.RowSpan="2" />
        <!--Clears all the waypoints in the waypoint list-->
        <Button x:Name="btnClearWaypoints" Click="ClearRoute" Grid.Row="1" HorizontalAlignment="Left" Content="Clear" Margin="10,32,0,-14" />
    </Grid>
</UserControl>
