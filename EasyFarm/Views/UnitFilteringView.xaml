<UserControl x:Class="EasyFarm.Views.UnitFilteringView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
        <GroupBox Header="Misc Settings" Foreground="White" Background="#FF2B2B2B" Margin="10">
            <StackPanel>
                <Label>Detection Distance</Label>
                <Slider Minimum="0" Maximum="100" Value="{Binding DetectionDistance}"></Slider>
                <Label>Height Threshold</Label>
                <Slider Minimum="0" Maximum="100" Value="{Binding HeightThreshold}"></Slider>
                <Label>Min Melee Distance</Label>
                <Slider Minimum="0" Maximum="100" Value="{Binding MinMeleeDistance}"></Slider>
                <Label>Max Melee Distance</Label>
                <Slider Minimum="0" Maximum="100" Value="{Binding MaxMeleeDistance}"></Slider>
                <Button Width="Auto" Margin="10" Command="{Binding RestoreDefaultsCommand}">Restore Defaults</Button>
            </StackPanel>
        </GroupBox>
    </ScrollViewer>
</UserControl>
